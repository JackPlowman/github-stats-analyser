name: Clean Caches

on:
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:

permissions: {}

jobs:
  clean-caches:
    name: Clean Caches
    permissions:
      contents: read
    uses: JackPlowman/reusable-workflows/.github/workflows/common-clean-caches.yml@6668fca8647a7d98f8cb9e665c32bd19d753a57b # v2026.03.07.01
    secrets:
      workflow_github_token: ${{ secrets.GH_TOKEN }}
