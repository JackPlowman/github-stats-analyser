name: Clean Caches

on:
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:

permissions: {}

jobs:
  clean-caches:
    name: Clean Caches
    permissions:
      contents: read
    uses: JackPlowman/reusable-workflows/.github/workflows/common-clean-caches.yml@b9caced30fcd8559d4729138891b23de5fbebfd7 # v2026.02.12.01
    secrets:
      workflow_github_token: ${{ secrets.GH_TOKEN }}
